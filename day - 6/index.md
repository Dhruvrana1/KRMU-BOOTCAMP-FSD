// React Features -:
virtual DOM 
single page application
JSX and WEBL
one way data binding
component based

short note = hooks are functions 
There are two rules to use hooks:
Only call hooks at the top level.
Only call hooks from React functions.